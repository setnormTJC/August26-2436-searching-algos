#pragma once


#include <string>
#include <vector>

//template <typename T> 
/*
* @param thangs -> a list of stuff to search through
* @return -1 if NOT found, 0 through size of array if FOUND
*/
int sequentiallySearch(const std::vector <std::string>& thangs,
	const std::string& thangToSearchFor);



